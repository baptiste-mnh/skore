PORT=4000
HOST=0.0.0.0
ALLOWED_ORIGINS="http://localhost:5173"

# Keyv - leave empty for in-memory (dev), set to redis://localhost:6379 for Redis (prod)
KEYV_URI=

# Room TTL in milliseconds (1 hour)
ROOM_TTL_MS=3600000

# Token secret for HMAC-based access tokens (256-bit secret)
# IMPORTANT: Generate a new secret for production using: openssl rand -base64 32
# DO NOT use this example value in production!
TOKEN_SECRET=change-this-to-a-random-secret-in-production
